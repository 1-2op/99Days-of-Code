//const  newNums = []
//myNums.forEach(  (num) => {
    //if ( num > 4) {
        //newNums.push(num)

    //}
//})
//console.log(newNums)

const myNumbers = [1, 2, 3, 4, 5]

const newNums = myNumbers.map(  (num) => num + 10)

 console.log(newNums);